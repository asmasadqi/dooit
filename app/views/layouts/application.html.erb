<!DOCTYPE html>
<html>
  <head>
    <title>Dooit</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>    
    <!-- display navbar on all pages except login, signup and password recovery pages-->
    <%- if controller_name != 'sessions' && controller_name != 'registrations' && controller_name != 'passwords' %>
      <%= render "shared/navbar" %>
    <% end %>
    <%= yield %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <!-- display footer on all pages except login, signup and password recovery pages-->
    <%- if controller_name != 'sessions' && controller_name != 'registrations' && controller_name != 'passwords' %>
      <%= render 'shared/footer' %>
    <% end %>
  </body>
</html>
